# MC Server World Backup

> Simple script that saves a backup of a given MC Server on a .zip file


## Usage
- yarn build or npm install
- node index.js [server folder path] [output folder path] [name of backup]